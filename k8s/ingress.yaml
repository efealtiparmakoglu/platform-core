apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-core-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: platform-core.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: platform-core-service
            port:
              number: 80
